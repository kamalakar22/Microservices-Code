apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-service-deployment
  namespace: default
  labels:
    app: api-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-service
  template:
    metadata:
      labels:
        app: api-service
    spec:
      containers:
      - name: api-service
        image: gkamalakar06/api-service:latest
        ports:
        - containerPort: 80
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: api-service-service
#   namespace: default
# spec:
#   selector:
#     app: api-service
#   ports:
#   - protocol: TCP
#     port: 80
#     targetPort: 80
#   type: LoadBalancer
